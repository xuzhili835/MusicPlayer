# Sakura Echo 详细设计文档

> 版本：1.3.0 ｜ 更新：2026-08-24 ｜ 定位：听力 / 播客 / 音乐学习伴侣（隐私优先）

---

## 1. 总体架构

```
┌───────────────────────────── 主进程 (main.js) ─────────────────────────────┐
│  BiliMusicPlayer 类（单例）                                                  │
│  ├─ 窗口管理：主窗口 / 桌面歌词窗口（data:URL 内联页）                        │
│  ├─ 隐私模式：globalShortcut 老板键 + 状态广播                                │
│  ├─ 下载管线：getVideoInfo → yt-dlp 下载 → ffmpeg 转 MP3 → 缩略图/歌词/音量   │
│  ├─ 转写管线：ffmpeg 转 16k wav → whisper-cli(-l auto -olrc) → LRC           │
│  ├─ executeCommand：统一子进程执行（异步/超时/取消/进度/网络参数注入）          │
│  └─ 模块：Database(sqlite3) / LyricsManager / ToolsManager                  │
└────────────────────────────────────────────────────────────────────────────┘
                    ▲ IPC (ipcMain.handle / webContents.send)
┌──────────────────────────── preload.js (contextBridge) ────────────────────┐
│  electronAPI: window/app/theme/database/file/download/desktopLyrics/        │
│               privacy/ocr/whisper/tools/lyrics/volume                      │
│  utils: 格式化/URL识别(B站+YT)/escapeHtml/debounce/...                      │
└────────────────────────────────────────────────────────────────────────────┘
                    ▼ 全局注入
┌──────────────────────────── 渲染进程 ──────────────────────────────────────┐
│  renderer.js：MusicPlayer 核心类（状态/初始化/事件接线/主题/确认框/消息）      │
│  mixin 挂载（Object.assign 到 prototype，src/ui/）：                         │
│   player-ui（播放引擎/模式/音量）  library-ui（库/视图/搜索/分类）            │
│   playlists-ui（歌单）             download-ui（下载界面）                   │
│   lyrics-ui（歌词/AI转写/OCR）     listening-ui（倍速/A-B/睡眠/随机队列）     │
│   context-menus（右键菜单）        dialogs-ui（设置/编辑/批量分析）           │
│  privacy.js：PrivacyMode（遮罩+暂停，订阅主进程老板键事件）                    │
└────────────────────────────────────────────────────────────────────────────┘
```

关键约束：
- `contextIsolation: true, nodeIntegration: false`，歌词窗口内联脚本只能用 preload 暴露的 `desktopLyrics` API
- 外部命令一律数组参数 spawn（无 shell），不使用 execSync（会阻塞主进程）
- 渲染进程所有用户数据插值必须经 `utils.escapeHtml`

## 2. 数据库（SQLite，userData/music.db）

| 表 | 字段 | 说明 |
|---|---|---|
| songs | id, title, artist, duration, path(UNIQUE), source_url, thumbnail, video_path, play_count, added_at, volume_gain, integrated_loudness, **content_type** | content_type ∈ music/podcast/listening，默认 music（v2 迁移） |
| playlists | id, name(UNIQUE), created_at | |
| playlist_songs | playlist_id + song_id 复合主键, order_index | 双外键 CASCADE |
| play_history | id, song_id(FK), played_at | |
| settings | key(PK), value(JSON), updated_at | |

迁移机制：`runMigrations()` 用 `PRAGMA table_info` 探测列后 `ALTER TABLE` 增量补齐（volume_gain → integrated_loudness → content_type），旧结构走 rebuildDatabase（备份有效歌曲→重建→恢复）。

### 设置键清单（settings 表）

| 键 | 默认 | 用途 |
|---|---|---|
| theme | dark | 主题 |
| volume_target_lufs | -16 | 音量同步目标响度 |
| volume_sync_enabled | true | 音量自动平衡开关 |
| privacy_settings | {enabled, keys:{overlay/overlay_minimize/audio_only/quit 各自键位，空=不绑定}, toolbarAction} | 多键位老板键与标题栏按钮动作 |
| music_dir | ''（默认用户数据目录/music） | 自定义音乐存储位置（迁移时自动更新 songs.path） |
| download_proxy | '' | yt-dlp/字幕下载代理 |
| cookies_path | '' | yt-dlp cookies.txt |
| whisper_model | small | 当前语音识别模型规格；旧配置保留用户原选择 |

渲染进程本地（localStorage）：volume、playMode、playbackRate。

## 3. 主要 IPC 通道

| 分类 | 通道 |
|---|---|
| 下载 | download-media / download-bilibili-video（别名）、download-get-video-info、**download-cancel**（kill 子进程+清理 _temp 文件）；推送 download-stage-progress、download-progress、lyrics-download-progress |
| 隐私 | privacy-toggle / privacy-get-settings / privacy-set-settings；推送 privacy-state-changed、privacy-shortcut-error |
| whisper | whisper-get-status / whisper-download-model / whisper-set-model / whisper-delete-model / transcribe-song；推送 whisper-model-progress、transcribe-status |
| 歌词 | lyrics-get（返回**解析后 [{time,text}] 数组**）/ lyrics-download / lyrics-save（带 source）/ **lyrics-match-online**（LRCLIB 在线匹配+校准）/ **lyrics-align**（重对齐）/ **lyrics-adjust-offset**（全局偏移微调）/ lyrics-window-*（toggle/update/lock-toggle）；推送 lyrics-download-progress、lyrics-lock-changed、lyrics-window-visibility |
| 存储 | storage-get-info / storage-choose-dir / storage-migrate（复制文件+更新DB+清理旧目录）；推送 storage-migrate-progress |
| 工具更新 | tools-check-update（本地版本 vs GitHub latest）/ tools-update-ytdlp |
| 主题 | theme-set / theme-get |
| 数据库 | songs/playlists/history/settings/stats 全套 CRUD |
| 音量 | volume-analyze-song / get-unanalyzed / get-stats / batch-update-gains |
| 文件 | file-select-music / file-select-cookies / check-songs-status / clean-missing-songs 等 |

## 4. 关键流程

### 4.1 下载（B站/YouTube 统一）
`用户粘贴 → extractMediaUrl 识别（B站/YT）→ getVideoInfo（yt-dlp --dump-json，注入 --proxy/--cookies）→ 去重(getSongByUrl) → yt-dlp --extract-audio --no-keep-video → ffmpeg 320k MP3 → 删除临时音频 → 缩略图本地化 → 入库(content_type=music) → EBU R128 分析 → 字幕下载（作者字幕优先，缺失时自动字幕；zh-Hans/zh/en，走代理）`。原始视频不保存；取消：download-cancel 置标志+kill 全部子进程+清理 `*_temp*`。字幕阶段通过 `lyrics-download-progress` 转发 yt-dlp 百分比，后续转换/保存显示 90%/96%/100%。

### 4.2 AI 音频转歌词
`右键 → transcribeSong → 检查 whisper-cli（缺则自动下载 ~2MB）→ 检查模型（缺则模型选择对话框）→ ffmpeg 转 16k 单声道 wav（*_temp_transcribe.wav）→ whisper-cli -m model -f wav -l auto -olrc -of prefix -pp → 读 LRC → 清理临时 → 编辑确认 → lyrics-save`。进度经 transcribe-status / download-progress(stdout) 推送。设置 → AI 歌词识别直接内嵌模型列表，可下载、切换或删除非当前模型；仅在右键识别遇到缺失模型时使用独立引导弹窗。带 source_url 的内容右键还可“尝试下载字幕/自动字幕”，用于 AI 转写失败时从平台获取更可靠的时间轴歌词。

### 4.3（已移除图片 OCR，应用户要求）

### 4.4 隐私模式（老板键，多键位）
四种动作（暂停遮罩/暂停+最小化/继续播放遮屏/直接退出）各自绑定独立全局快捷键（可任意组合）；遮罩中按任一键恢复；标题栏按钮与 Ctrl+Shift+H 走 toolbarAction。录制键位时必须先 privacy-suspend-shortcuts 注销全局键（否则按键在系统层被拦截直接触发动作——曾致覆盖录制失效）。

### 4.5 播放模式与随机队列
单按钮三态：sequence → shuffle → single。shuffle 模式维护 `shuffleOrder[]`（当前曲开头 + Fisher-Yates）：next 沿队列前进（一轮完自动重洗），prev 沿队列回退（到头停住）；playSong 后 `syncShufflePosition` 对齐。

### 4.6 歌词获取与时间轴校准（lyrics.js）
获取链路：LRCLIB 在线匹配优先（`cleanTitleForLyrics` 清洗 B 站搬运标题：书名号内优先作歌名 → 剥【】装饰段 → 拆 A-B 艺术家 → 去搬运后缀），命中即下载同步歌词；无命中回退本地 whisper 转写。

校准算法（`computeAlignedTimes(lrc, wav, audioDuration)`）：
- **同版本门禁**：|本地时长 − 在线末句| ≤ 2s ⇒ 仅全局微调（中位偏移，clamp ±2.5s），**不做任何分段偏移**——版本一致时个别抖动锚点不可信
- 结构差异版：单调配对（bigram Dice 相似度）→ 锚点中位数全局偏移 + **孤峰剔除**（内部锚与两侧邻差均 >3s 即剔，不问方向）+ 未配对句按相邻锚点**分段线性插值**
- 放宽配对窗（10s→20s）仅当行数 ≥8 且配对率 <50%

### 4.7 后台任务面板（src/ui/download-ui.js）
标题栏徽标显示进行中任务数，点击展开面板：下载 / AI 识别 / 模型下载统一注册表管理（独立名称、状态文本、进度条、取消按钮），取消统一走主进程 kill 子进程 + 清理临时文件；完成任务 4 秒后自动移出面板。

## 5. 外部工具与模型（tools-manager.js）

| 工具/资源 | 来源 | 位置 | 说明 |
|---|---|---|---|
| yt-dlp.exe | GitHub Release（+镜像） | bin/（应用）→ userData/bin（用户）→ PATH | 启动自动检查 |
| ffmpeg/ffprobe.exe | gyan.dev / BtbN（+镜像，zip） | 同上 | extract-zip 解压（adm-zip 已弃用） |
| whisper-cli.exe | whisper.cpp v1.7.6 release（+镜像） | 同上 | **按需下载**（首次转写时） |
| whisper 模型 | hf-mirror / HF（ggml-*.bin） | userData/models/ | **用户选择规格下载**（tiny/base/small/medium，默认推荐 Small），.part 防半截；当前模型禁止删除，任一非当前已下载模型可删除 |

## 6. UI 设计体系（styles.css）

- 双主题：`html[data-theme="dark"|"light"]` CSS 变量（bg/surface/text/border/accent 樱花粉...），主题持久化 DB
- 统一组件：icon-btn/btn 系列、单一 modal 系统（modal-overlay/content/header/body/footer）、popover-menu、context-menu、toast(message)、自定义滑块、空状态按视图动态渲染
- 无障碍：图标按钮 aria-label、列表项 role=option + 键盘 Enter/Space 播放、:focus-visible

## 7. 已知设计决策 / 注意事项

- 播放停止权在渲染进程（audio 元素在其侧）；删除文件前由渲染端先停播释放句柄，主进程不做 executeJavaScript 反控
- lyrics-get 必须返回数组（解析 LRC）；纯文本歌词（OCR 导入）以 time:null 行保存，桌面歌词显示首行
- LRC 解析会过滤带时间轴的制作元数据（作词、作曲、编曲、作者、作家等）；无有效歌词时桌面窗口显示“暂无有效歌词”，不会保留旧行。桌面歌词使用 `-webkit-app-region: drag` 原生拖动，不经 IPC 持续跟踪鼠标。
- PowerShell 脚本写入必须带 UTF-8 BOM；bash→powershell 传路径会被 MSYS 转换破坏（仅影响测试，应用内 Node spawn 无此问题）
- content_type 字段保留在 DB 但 UI 已下线（应用户要求用歌单组织）
- 音乐目录可自定义（设置 music_dir），启动时数据库先初始化再读目录配置；迁移=复制+UPDATE path+清理旧目录
- yt-dlp 用户目录版本优先于内置版本（更新通道）；内置版本为打包兜底；B站 412=旧版extractor+缺buvid cookie，已内置 finger/spi 领匿名 cookie
- 弹出菜单（倍速/睡眠）触发按钮必须 stopPropagation，否则冒泡到 document 关闭器导致菜单闪关
- Electron 锁定 38.x：37 及以下携带透明窗口失焦白框回归（electron/electron#47946，35.5.0 引入、修复随 38 发布未 backport），叠加已禁用 CalculateNativeWinOcclusion（遮挡误判→白屏）与 DWM 边框色 NONE；sqlite3 为 N-API 模块跨 Electron 版本免重编译，node-abi 需 ≥3.94（识别 38 的 ABI）
