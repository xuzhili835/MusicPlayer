# Music Dreamer 微服务 详细设计文档

> 版本：v1.1（单文档整合版）｜ 日期：2026-08-17
> 项目：**Music Dreamer**（后端工程名 musicback）
> 源项目（能力复用来源）：Sakura Echo —— https://github.com/xuzhili835/MusicPlayer（本文所有命令、SQL、算法均在该项目实测通过）
> 基线：Spring Boot 2.2.2 + Spring Cloud Hoxton + Spring Cloud Alibaba 2.1.0 + Gateway + Nacos 1.1.4 + MySQL 8 + Redis + MyBatis-Plus + JWT
> 本次范围：① 媒体处理服务（yt-dlp/ffmpeg/whisper 工具链）② 播放统计体系 ③ 设置中心 ④ 监控告警 ⑤ Windows Docker 容器化

---

## 1. 总体架构

### 1.1 架构图

```
                        ┌──────────────────────────────────────────┐
                        │  Gateway :80 (路由/鉴权，新增 /media/**)  │
                        └───────┬───────────────────────────┬──────┘
                                │ Nacos 服务发现             │
   ┌──────────┬──────────┬──────┴─────┬─────────┬───────────┴─┬─────────┐
   │Mod_login │Mod_music │ Mod_media  │Mod_upld │Mod_songList │Mod_like │ …existing
   │  :8001   │  :8002   │  :8010 ★   │  :8007  │  :8008      │  :8009  │
   └──────────┴──────────┴─────┬──────┴─────────┴─────────────┴─────────┘
                             │ 子进程(spawn数组参数)
                   ┌─────────┴─────────┐
                   │ yt-dlp/ffmpeg/    │  开发期=宿主机现有工具(PATH探测)
                   │ whisper.cpp       │  容器期=镜像内Linux版
                   └─────────┬─────────┘
                             │ 文件落盘 /data + Feign 写库
   基础设施(Compose): MySQL:3306  Redis:6379  Nacos:8848
   监控(Compose):    Prometheus:9090  Grafana:3000  Alertmanager:9093
```

### 1.2 模块增量

| 模块 | 端口 | 服务名 | 状态 | 职责 |
|---|---|---|---|---|
| Mod_media | 8010 | mediaService8010 | **新增** | 下载/转码/响度/转写/字幕/工具与模型管理 |
| Mod_music | 8002 | musicService8002 | 增强 | 播放上报、历史/最近/榜单、响度字段下发 |
| Mod_setting | 8005 | settingService8005 | 增强 | 系统键值配置中心 |
| domain | - | - | 增强 | 新实体/工具类 |
| infra | - | - | 新增 | compose/Dockerfile/监控配置 |

> Mod_media 独立成服务的原因：子进程 CPU 密集（转码/转写分钟级）、生命周期与 HTTP 请求不同步（必须任务化）、镜像大（工具链）——与在线服务隔离、独立伸缩。

---

## 2. 数据库设计

### 2.1 现有表扩展

```sql
ALTER TABLE music ADD COLUMN source_url VARCHAR(512) NULL COMMENT '下载来源URL(查重)';
ALTER TABLE music ADD COLUMN volume_gain DOUBLE NULL COMMENT '响度补偿增益dB';
ALTER TABLE music ADD COLUMN integrated_loudness DOUBLE NULL COMMENT '原始响度LUFS';
ALTER TABLE music ADD INDEX idx_music_source (source_url);
-- listen_numb 保留为冗余热计数；明细走 play_history
```

### 2.2 新表

```sql
CREATE TABLE media_task (
  id INT PRIMARY KEY AUTO_INCREMENT,
  task_type VARCHAR(20) NOT NULL COMMENT 'DOWNLOAD/TRANSCRIBE/LOUDNESS/SUBTITLE/MODEL_DOWNLOAD/TOOL_UPDATE',
  status VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING/RUNNING/SUCCESS/FAILED/CANCELLED',
  progress INT NOT NULL DEFAULT 0 COMMENT '0-100',
  stage VARCHAR(200) NULL COMMENT '当前阶段描述(中文，直接展示)',
  source_url VARCHAR(512) NULL,
  music_id INT NULL,
  operator INT NULL COMMENT '发起用户id',
  error VARCHAR(1000) NULL COMMENT '转译后的用户可读错误',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  finished_at DATETIME NULL,
  INDEX idx_task_status(status), INDEX idx_task_music(music_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE play_history (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL, music_id INT NOT NULL,
  played_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_ph_music(music_id), INDEX idx_ph_time(played_at), INDEX idx_ph_user(user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE sys_setting (
  cfg_key VARCHAR(100) PRIMARY KEY,
  cfg_value TEXT NOT NULL COMMENT 'JSON值',
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

现有 `song_list`/`list_music` 与源项目歌单设计等价，不动。

---

## 3. Mod_media：进程执行器（基础设施）

一切工具调用的唯一出口，六条强约束（源项目实测教训）：

1. **数组参数**，绝不拼 shell 字符串（文件名含引号/`&` 即注入）
2. **全异步**（ProcessBuilder + CompletableFuture）；严禁同步阻塞（源项目曾因同步跑 ffmpeg 冻结整个服务）
3. **stderr 必须收集**——ffmpeg ebur128 结果在 stderr 不在 stdout
4. stdout **按行回调**（进度解析），累计缓冲设上限（dump-json 异常巨输出防内存爆）
5. 超时 = 定时器 kill；**取消 = 任务注册表登记的进程 kill + 清理 `*_temp*` 半成品**
6. 输出编码统一 UTF-8；stdout/stderr 分开收

```java
CompletableFuture<ExecResult> run(List<String> cmd, long timeoutMs,
                                  Consumer<String> onLine, String cancelKey);
// ExecResult { int exitCode; String stdout; String stderr; boolean timedOut; }
```

进度节流：行回调里解析出的 progress 以 ≥500ms 间隔才写 media_task（避免高频 UPDATE）。

---

## 4. 工具管理（含自动检查更新）★详细

### 4.1 工具探测（ToolsLocator，启动时 + 调用时兜底）

解析顺序：
1. `sys_setting.tools_path`（显式配置）
2. 系统 `PATH`（开发机 winget 已装 ffmpeg 7.1.1 / yt-dlp 2026.07——**开发期默认即命中**）
3. 约定目录（开发机 Sakura bin：`D:\Desktop\新建文件夹\SakuraEchotesting\Sakura-Echo-github\bin`，全套三件兜底）

启动自检（@Async 不阻塞启动）：逐工具执行 `--version`（5s 超时）→ 内存缓存 + Gauge 指标 `media_tool_alive{tool}` + 失败告警。

### 4.2 版本检查（三个触发时机）

| 时机 | 实现 | 失败处理 |
|---|---|---|
| 服务启动后 30s | @Async 单次，静默 | 记 WARN 日志，不影响启动 |
| 每日 09:00 | @Scheduled(cron="0 0 9 * * ?") | 同上 |
| 管理员手动 | GET /media/tools/status | 前端展示错误 |

**检查流程（yt-dlp；ffmpeg 稳定不做自动更新）**：
```
① 本地版本：exec <ytdlpPath> --version（5s超时）→ 如 "2026.07.04"
② 远程版本：GET https://api.github.com/repos/yt-dlp/yt-dlp/releases/latest
   Headers: User-Agent: MusicDreamer-Updater, Accept: application/vnd.github+json
   超时10s，3xx跟随；解析 tag_name（yt-dlp 版本号即日期，字典序可比较）
③ 本地 ≠ 远程 → 有新版
```

### 4.3 自动更新流程（TOOL_UPDATE 任务）

```
管理员确认 → 建 media_task(TOOL_UPDATE, RUNNING)
① 下载: GET https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp.<平台>
   流式写 <tools>/yt-dlp.new.part，按 Content-Length 算进度→task.progress
   失败重试镜像源: https://ghproxy.com/https://github.com/...（源列表依次切换）
② 校验: 文件 >5MB（拦截半截/错误页）
③ 原子替换: 旧文件 rename → yt-dlp.bak；.part rename → 正式名
   再 exec --version 验证；验证失败回滚 .bak，任务 FAILED
④ 结果: task SUCCESS + Feign→Mod_msg 管理员站内信 + 指标 media_tool_update_total{result}
```

### 4.4 whisper 模型管理（生命周期）

规格常量表（**全部多语言**，区别于 *.en 纯英文变体）：

| key | 体积 | 定位 |
|---|---|---|
| base | ~142MB | 默认推荐 |
| small | ~466MB | 高精度 |
| medium | ~1.5GB | 最高/很慢 |
| tiny | ~75MB | 试水 |

- **列表** `GET /media/models`：每规格 {key,label,desc,sizeText,downloaded,sizeBytes,isCurrent}
- **下载** MODEL_DOWNLOAD 任务（**必须用户确认规格才触发，绝不静默**）：
  `https://hf-mirror.com/ggerganov/whisper.cpp/resolve/main/ggml-<key>.bin` → 失败切 `huggingface.co` → `.part` 流式 + 实时进度 → 校验 >1MB → rename → 登记
- **切换** `POST /media/models/{key}/use`：更新 sys_setting.whisper_model（下次任务生效）
- **删除** `POST /media/models/{key}/delete`：非当前使用才允许，删文件释放空间

---

## 5. 下载管线（DOWNLOAD 任务）★详细

### 5.1 提交与查重

```
POST /media/download {url, wantSubtitle=true, wantTranscribe=false}
① URL 识别（正则族）:
   B站: bilibili.com/video/ | m.bilibili.com | b23.tv | 裸BV号 | av号
   YT: watch?v= | shorts/ | live/ | youtu.be | music.youtube.com
   不识别 → 400 "不支持的链接"
② 查重: SELECT music_id FROM music WHERE source_url=? → 命中 409 {musicId}（前端直接跳播放）
③ INSERT media_task(PENDING, DOWNLOAD) → 立即返回 {taskId}
```

### 5.2 Worker 执行（下载单并发；转写并发=CPU核数-1）

| 阶段 | 命令（数组参数；网络参数见5.3） | 超时 | 失败处理 |
|---|---|---|---|
| ①元数据 | `yt-dlp --dump-json --no-playlist <net> <url>` | 30s | 412→重试1次→仍失败按§6转译 |
| ②下载 | `yt-dlp --extract-audio --audio-format best --audio-quality 0 --output /data/task/<id>_temp.%(ext)s --no-playlist <net> <url>` | 20min | stdout 解析 `[download] xx.x%`（progress 0-60） |
| ③转码 | `ffmpeg -i <temp> -codec:a libmp3lame -b:a 320k -y /data/music/<musicId>.mp3` | 10min | progress 60-75 |
| ④元数据 | 时长取①JSON；缩略图 http 流式落地 /data/image/<musicId>.jpg | 10s | 失败仅告警不阻断 |
| ⑤入库 | Feign→Mod_music 建记录（含 source_url，volume_gain=NULL） | 5s | 失败任务 FAILED+清理文件 |
| ⑥后置 | LOUDNESS 入队；wantSubtitle→SUBTITLE 入队；wantTranscribe 且模型就绪→TRANSCRIBE 入队 | - | 模型未就绪跳过并在 stage 说明 |

完成 → SUCCESS + finished_at；**取消**（任意阶段）→ kill 进程 + 删 `<id>_temp*` + CANCELLED。

### 5.3 网络参数注入（每个 yt-dlp 命令统一拼装）

```
必带: --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36"
B站URL: --add-headers "Referer:https://www.bilibili.com/"
YT URL:  --add-headers "Referer:https://www.youtube.com/"
sys_setting.download_proxy 非空 → --proxy <值>
sys_setting.cookies_path 非空 → --cookies <值>（用户级最高优先）
否则 B站URL → 自动 buvid cookie（§6）
```

### 5.4 进度推送协议

前端 `GET /media/task/{id}` 1s 轮询：`{status,progress,stage,musicId,error}`，stage 全程中文（"正在下载文件... 45%"）。可后续升级 SSE，轮询为课设稳妥默认。

---

## 6. B站 412 风控（必抄，两层缺一不可）

**现象**：老版 yt-dlp 或无 cookie 请求 B站 API → `HTTP 412 Precondition Failed`（2024 年起收紧；表现为"以前能下现在不行"）。

**层1**：yt-dlp 保持新版（§4 自动更新机制保障）。

**层2**：自动领匿名 cookie（缓存 1 小时）：
```
GET https://api.bilibili.com/x/frontend/finger/spi
Headers: User-Agent=<Chrome UA>, Referer=https://www.bilibili.com/
→ 200 {code:0, data:{b_3:"...", b_4:"..."}}

写 Netscape 格式（TAB 分隔，存 /data/task/bili_cookies.txt）:
.bilibili.com\tTRUE\t/\tFALSE\t<expire秒级时间戳>\tbuvid3\t<b_3>
.bilibili.com\tTRUE\t/\tFALSE\t<expire秒级时间戳>\tbuvid4\t<b_4>
→ 命令追加 --cookies <该文件>
```

**错误转译表**（写 task.error，绝不把原始 stderr 给用户）：

| 匹配 | 用户可读信息 |
|---|---|
| 412 / Precondition | 站点风控拦截，请稍后重试；反复出现请配置代理 |
| 403 | 站点拒绝访问，可能需要登录（配置 cookies.txt） |
| timeout / 超时 | 网络超时，请检查网络或代理设置 |
| Video unavailable | 视频不存在或已删除 |
| Private video | 视频为私有，无法下载 |

---

## 7. 响度分析（EBU R128）

```
命令: ffmpeg -i <file> -filter_complex ebur128 -f null -     ★输出在 stderr
解析(两级正则，实测):
  主: /Summary:[\s\S]*?Integrated loudness:[\s\S]*?I:\s+([-\d]+\.?\d*)\s+LUFS/is
  兜底: /I:\s+(-?\d+\.?\d*)\s+LUFS/i
计算: gain = target(-16默认) - integrated
写库: UPDATE music SET volume_gain=?, integrated_loudness=? WHERE music_id=?
```

- **触发**：下载后自动入队 + 手动单曲 + 管理员批量（`WHERE volume_gain IS NULL`）
- **改目标响度**（sys_setting.volume_target_lufs 变更时）：`UPDATE music SET volume_gain = <newTarget> - integrated_loudness WHERE integrated_loudness IS NOT NULL`——**免重新分析**
- **播放端**：接口下发 volume_gain，前端 `volume = base × 10^(gain/20)` clamp[0,1]；无增益数据回退原音量

---

## 8. AI 转写与字幕（TRANSCRIBE / SUBTITLE）

### 8.1 转写（对已有 music 也适用——存量无需重传）
```
① 预处理: ffmpeg -i <mp3> -ar 16000 -ac 1 -y <temp.wav>
② 识别: whisper-cli -m /models/ggml-<当前规格>.bin -f <wav> -l auto -olrc -of <prefix> -pp
        (-l auto 中英日俄法德等99语言自动检测；-pp 输出 "progress = XX%" 行)
③ prefix.lrc → /data/lyric/<musicId>.lrc → UPDATE music.lyric
④ 清理 wav 等临时文件
模型未下载 → task FAILED "请先在设置中下载模型"（前端引导模型管理页）
```

### 8.2 字幕下载
```
yt-dlp --write-subs --skip-download --sub-lang "zh-Hans,zh,en" --output "<title>.%(ext)s" <net> <url>
按语言优先级找产物: <title>.zh-Hans.srt → .zh.srt → .en.srt → 无后缀.srt（.vtt/.ass 同理）
无字幕 → 静默结束（不算失败），前端提示"该视频没有字幕，可用AI识别"
```

### 8.3 字幕 → LRC 转换（源项目 lyrics.js 可作 Java 移植参考）
- SRT：跳过纯数字序号行；`HH:MM:SS,mmm --> ...` 取**起始**时间；后续非空行拼接为一句
- VTT：跳 `WEBVTT`/`NOTE` 头，余同 SRT
- ASS：`Dialogue:` 行 split(',')，第2列 `H:MM:SS.CC` 起始，第10列起 join 并剥 `{...}` 特效标签
- 时间统一 `[mm:ss.xx]`；LRC 解析正则 `/\[(\d{2}):(\d{2})\.(\d{2})\](.*)/` 按 time 排序
- 纯文本（无时间标签）按行保留，time 置 null（前端整段展示）

---

## 9. Mod_media API 汇总（Mess 风格）

| 接口 | 方法 | 鉴权 | 说明 |
|---|---|---|---|
| /media/download | POST | 登录 | 提交下载 `{url,wantSubtitle,wantTranscribe}`→`{taskId}`；重复返回409 |
| /media/task/{id} | GET | 登录 | 轮询 `{status,progress,stage,musicId,error}` |
| /media/task/{id}/cancel | POST | 登录 | kill+清理temp |
| /media/transcribe/{musicId} | POST | 登录 | 存量AI转写（建任务） |
| /media/loudness/{musicId} | POST | 登录 | 单曲响度分析 |
| /media/loudness/batch | POST | 管理员 | 批量分析未分析曲目 |
| /media/tools/status | GET | 管理员 | 各工具版本+健康+最新版比对结果 |
| /media/tools/update-ytdlp | POST | 管理员 | 触发 TOOL_UPDATE 任务 |
| /media/models | GET | 登录 | 模型列表 |
| /media/models/{key}/download \| use \| delete | POST | 管理员 | 下载/切换/删除 |

网关路由追加：`Path=/media/** → lb://mediaService8010, filters:[GetUserId]`（工具/模型管理接口在服务内再校验 role=0）。

---

## 10. 播放统计体系（Mod_music 增强）

```
POST /music/playReport {musicId}   // 前端 audio ended 触发（播完才计，点开不算）
  → INSERT play_history + UPDATE music SET listen_numb=listen_numb+1（同一事务）

GET /music/recent?limit=50  最近播放（去重取最新）:
  SELECT m.*, MAX(h.played_at) last FROM music m JOIN play_history h
    ON m.music_id=h.music_id WHERE h.user_id=?
    GROUP BY m.music_id ORDER BY last DESC LIMIT ?
GET /music/history?page&size      个人播放明细分页
GET /music/chart?type=hot&limit   全站榜 ORDER BY listen_numb DESC
GET /music/chart?type=rise        周榜: play_history 7天 COUNT 分组
@Scheduled 每日清理: 保留最近1万条 (DELETE ... NOT IN (SELECT id ... ORDER BY played_at DESC LIMIT 10000))
```

---

## 11. 设置中心（Mod_setting）

sys_setting 初始键（值存 JSON，新键零迁移）：

| cfg_key | 默认 | 说明 |
|---|---|---|
| tools_path | "" | 工具目录覆盖（空=PATH探测） |
| download_proxy | "" | yt-dlp 代理 |
| cookies_path | "" | 用户级 cookies.txt |
| volume_target_lufs | -16 | 响度目标（变更即触发§7批量重算） |
| whisper_model | base | 当前模型规格 |
| storage_root | /data | 存储根（music/image/lyric/task） |
| alert_email_to | "" | 告警收件箱（复用现有邮件能力） |

API：`GET /setting/all`、`POST /setting/{key}`（管理员）。**即时生效**：Mod_media 每任务实时读取（本地缓存≤60s）。

---

## 12. 熔断降级设计

### 12.1 组件选型：Sentinel

与 Nacos 同属 Spring Cloud Alibaba 生态（版本配套无冲突），注解式接入 + 独立控制台（可动态调规则、好演示）；Hystrix 已进维护模式不选。

依赖与配置：
```xml
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
</dependency>
```
```yaml
spring.cloud.sentinel:
  transport: { dashboard: "sentinel:8858", port: "8719" }
feign.sentinel.enabled: true   # Feign 调用自动纳入熔断
```
compose 追加：`sentinel: { image: bladex/sentinel-dashboard, ports: ["8858:8858"] }`（账号 sentinel/sentinel）。

### 12.2 熔断点分析（哪些调用需要保护）

| 调用链 | 风险 | 保护方式 |
|---|---|---|
| Mod_media →Feign→ Mod_music/Mod_msg | 对方宕机/慢 → 任务卡死 | Sentinel 熔断 + fallback |
| Gateway → 各业务服务 | 单服务故障拖垮入口线程 | 网关超时 + 熔断 |
| Mod_media → 外部站点（B站/GitHub/HF） | 412风控/网络抖动 → 任务连续失败 | **自实现站点级断路器**（Sentinel 管不了外部业务语义） |
| POST /media/download 提交口 | 恶意刷接口堆任务 | Sentinel 限流（QPS） |

### 12.3 降级策略（按语义分层，不是一刀切）

| 类型 | 策略 |
|---|---|
| 查询类（详情/榜单） | Redis 缓存兜底（热门数据 TTL 10min）→ 无缓存返回"数据暂时不可用" |
| 写入类（Feign 建库失败） | 任务**不失败**——status 回 PENDING，@Scheduled 每 30s 重试 3 次，仍失败才 FAILED |
| 消息通知失败 | 仅记日志（通知永远不阻塞主流程） |
| 外部站点熔断 | 连续 5 次任务失败(412/超时) → 断路器 OPEN 快速失败 10min → 半开放行 1 个探测任务 → 成功 CLOSE。状态存内存 + 指标 media_site_breaker{site,state} |

Feign fallback 写法（domain 模块统一提供）：
```java
@FeignClient(name = "musicService8002", fallback = MusicClientFallback.class)
public interface MusicClient {
    @PostMapping("/music/internal/create")
    Mess create(@RequestBody Music music);
}
@Component
public class MusicClientFallback implements MusicClient {
    public Mess create(Music music) {
        return Mess.fail().mess("音乐服务暂不可用，任务稍后自动重试"); // 任务层转 PENDING 重试
    }
}
```

### 12.4 Sentinel 规则（控制台动态配置，课设演示点）

| 资源 | 规则 |
|---|---|
| /media/download | QPS 限流 2/s（单用户 1/s，基于网关透传 id 头） |
| Feign:musicService8002 | 异常比例 >50%（窗口10s）→ 熔断 30s，最小请求 5 |
| Feign:msgService8006 | 慢调用比例（RT>1s 占 60%）→ 熔断 60s |
| /music/**（网关侧） | QPS 200/s 总闸 |

---

## 13. 单点故障分析（SPOF）与高可用对策

### 13.1 组件级分析表

| 组件 | 单点风险 | 故障影响 | 课设对策 | 生产演进 |
|---|---|---|---|---|
| **MySQL** | ★★★ 唯一状态库 | 全部业务不可用 | 卷持久化 + 每日 mysqldump 定时备份（保留7份）+ restart:always | 主从复制 + MHA |
| **Redis** | ★★ 缓存/验证码 | 验证码失效、热门查询变慢 | **降级直查 DB**（Redis 异常 catch 走库）+ restart:always | 哨兵/集群 |
| **Nacos** | ★★ 注册中心 | **已运行服务间调用不受影响**（客户端缓存服务列表）；新实例无法注册 | derby 数据目录挂卷 + restart:always | 3节点集群(内置Raft) |
| **Gateway** | ★★★ 唯一入口 | 全部外部流量中断 | restart:always；说明多实例方案（多端口+Nginx轮询） | LB + 多实例 |
| **Mod_media Worker** | ★★ 任务执行器 | 下载/转写中断 | **任务表自愈（13.2）** + restart:always | 多实例乐观锁抢任务 |
| **存储卷 /data** | ★★ 文件唯一副本 | 音乐/封面/歌词丢失 | 定期备份到宿主另一目录 | NAS/对象存储 |
| **外部站点** | 不可控 | 下载/更新失败 | §12.3 站点断路器 + 镜像源 + 明确错误提示 | 同左 |
| **邮件服务** | 不可控 | 告警收不到 | 通知异步化；可加钉钉 Webhook 双通道 | 同左 |

### 13.2 任务自愈机制（Mod_media 重启恢复）★

media_task 落库是任务可靠性根基——**进程可死，任务状态不丢**：

```
服务启动时（ApplicationRunner）：
① 遗留 RUNNING（崩溃时进程被杀）：
   UPDATE media_task SET status='FAILED',
     error='服务重启导致任务中断，请重新提交' WHERE status='RUNNING'
   （多实例演进：RUNNING 记 worker 标识+心跳时间，超时才判死）
② 遗留 PENDING：保留，Worker 启动后自然消费——无需人工干预
③ 清理孤儿临时文件：扫描 /data/task/*_temp* 删除（进程死了没人清）
④ 工具自检（§4.1）通过后 Worker 才开始接任务
```

### 13.3 编排层高可用（compose 强化）

```yaml
# 所有服务统一 restart: always
# 基础设施健康检查 + 启动顺序依赖（替代 sleep，更可靠）：
mysql:
  healthcheck: { test: ["CMD","mysqladmin","ping","-h","localhost"], interval: 10s, retries: 10 }
redis:
  healthcheck: { test: ["CMD","redis-cli","ping"], interval: 10s, retries: 10 }
nacos:
  healthcheck: { test: ["CMD-SHELL","curl -f http://localhost:8848/nacos || exit 1"], interval: 15s, retries: 10 }
mod-media:
  depends_on:
    mysql: { condition: service_healthy }
    redis: { condition: service_healthy }
    nacos: { condition: service_healthy }
```

### 13.4 故障演练场景（答辩演示脚本）

| 演练 | 操作 | 预期 |
|---|---|---|
| 业务服务宕机 | docker stop mod-music | 任务入库失败→自动 PENDING 重试；恢复后自动完成 |
| 熔断触发 | Sentinel 控制台压低异常比例阈值制造失败 | 熔断事件 + fallback 生效 + Grafana 可见 |
| 断电级重启 | docker compose restart | 任务自愈、PENDING 续跑、临时文件清理 |
| MySQL 恢复 | 停库→起库 | Druid 自动重连，备份文件演示 |
| 工具损坏 | 删容器内 yt-dlp | tool_alive 告警 → 管理接口重装 |

---

## 14. 监控告警


### 14.1 数据流
```
各服务(Actuator+Micrometer) --/actuator/prometheus--> Prometheus:9090
  --rules.yml--> Alertmanager:9093 --SMTP--> alert_email_to（复用 Commons-Email）
Prometheus --> Grafana:3000（预置看板JSON）
```

依赖：各服务 pom 加 `spring-boot-starter-actuator` + `micrometer-registry-prometheus`；`management.endpoints.web.exposure.include: health,info,prometheus`。

### 14.2 业务指标（Mod_media 重点）

| 指标 | 类型 | 用途 |
|---|---|---|
| media_task_total{type,status} | Counter | 任务量/成功率/412趋势 |
| media_task_duration_seconds{type} | Timer | 转码/转写耗时分布 |
| media_task_queue_size | Gauge | 积压告警 |
| media_tool_alive{tool} | Gauge | 工具健康（启动自检探针） |
| media_process_running | Gauge | 存活子进程数（防泄漏兜底） |
| media_tool_update_total{result} | Counter | 工具更新成败 |
| JVM/HTTP 指标 | 自动 | 标配 |

### 14.3 告警规则
```yaml
- alert: ServiceDown        # up{job=~"mod_.*"}==0 for 30s
- alert: TaskQueueBacklog   # media_task_queue_size>10 for 2m
- alert: TaskFailureSpike   # rate(media_task_total{status="FAILED"}[5m])>0.2 for 5m
- alert: HighJvmGc          # rate(jvm_gc_collection_seconds_count{gc="PS MarkSweep"}[5m])>1 (JDK8老年代GC，本项目版本常见)
- alert: ToolUnhealthy      # media_tool_alive==0 for 5m
```

---

## 15. 容器化部署（Windows Docker）

### 13.1 compose（骨架）
```yaml
version: "3.8"
services:
  mysql: { image: mysql:8.0, environment: {MYSQL_ROOT_PASSWORD: "123456", MYSQL_DATABASE: music},
           command: --character-set-server=utf8mb4, volumes: ["mysql_data:/var/lib/mysql","./init:/docker-entrypoint-initdb.d"], ports: ["3306:3306"] }
  redis: { image: redis:6, ports: ["6379:6379"] }
  nacos: { image: nacos/nacos-server:1.1.4, environment: {MODE: standalone}, ports: ["8848:8848"] }
  # 业务服务共用模板镜像，环境变量区分：
  mod-login: { build: {context: ., dockerfile: Dockerfile.service}, environment: {SVC: Mod_login, PORT: 8001}, depends_on: [mysql,redis,nacos] }
  # …… 其余 Mod_* 同模式 ……
  mod-media:
    build: {context: ., dockerfile: Dockerfile.media}
    volumes: ["media_data:/data","tools:/opt/tools","models:/models"]
    # 开发机已有 whisper 模型可直挂宿主目录: "D:/whisper-models:/models"
    depends_on: [mysql,redis,nacos]
  gateway: { build: {context: ., dockerfile: Dockerfile.service}, environment: {SVC: music_gateway, PORT: 80}, ports: ["80:80"], depends_on: [nacos] }
  prometheus: { image: prom/prometheus, volumes: ["./monitor/prometheus.yml:/etc/prometheus/prometheus.yml","./monitor/rules.yml:/etc/prometheus/rules.yml"], ports: ["9090:9090"] }
  alertmanager: { image: prom/alertmanager, volumes: ["./monitor/alertmanager.yml:/etc/alertmanager/alertmanager.yml"], ports: ["9093:9093"] }
  grafana: { image: grafana/grafana, environment: {GF_SECURITY_ADMIN_PASSWORD: admin}, volumes: ["grafana_data:/var/lib/grafana","./monitor/dashboards:/var/lib/grafana/dashboards"], ports: ["3000:3000"] }
volumes: { mysql_data: {}, media_data: {}, tools: {}, models: {}, grafana_data: {} }
```

### 13.2 镜像
```dockerfile
# Dockerfile.service（通用业务服务）
FROM eclipse-temurin:8-jre
ARG SVC
COPY ${SVC}/target/*.jar /app.jar
ENTRYPOINT ["java","-Xmx512m","-Dfile.encoding=UTF-8","-jar","/app.jar"]

# Dockerfile.media（工具链）
FROM eclipse-temurin:8-jre
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg curl ca-certificates && rm -rf /var/lib/apt/lists/*
ENV TOOLS_DIR=/opt/tools
RUN mkdir -p ${TOOLS_DIR} /data/music /data/image /data/lyric /data/task /models \
    && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_linux -o ${TOOLS_DIR}/yt-dlp && chmod +x ${TOOLS_DIR}/yt-dlp
COPY Mod_media/target/*.jar /app.jar
ENTRYPOINT ["java","-Xmx1g","-Dfile.encoding=UTF-8","-jar","/app.jar"]
```

### 13.3 宿主机工具复用（分场景）★
- **开发期（本地跑 Spring Boot）零下载**：ToolsLocator 探测 PATH → 开发机 winget 已装 ffmpeg 7.1.1 + yt-dlp（另有 Sakura bin 全套兜底：`D:\Desktop\新建文件夹\SakuraEchotesting\Sakura-Echo-github\bin`）
- **容器期**：Linux 容器**不能执行 Windows exe**（关键坑）——ffmpeg 镜像 apt 装、yt-dlp 镜像内下载 Linux 版；**whisper ggml 模型 / cookies.txt 是跨平台数据文件，可挂载复用**

### 13.4 Windows 注意
- Docker Desktop（WSL2 后端）；容器互连把配置里的 `localhost` 改为服务名（mysql/redis/nacos）——**容器化改造最高频错误**
- MySQL 强制 utf8mb4；JVM `-Dfile.encoding=UTF-8`
- Mod_upload 的 `download.address` 从 `C:/file/` 改为卷 `/data`

---

## 16. 非功能与规范

- SQL 一律参数化（`#{}`）；用户输入的标题等**输出时前端转义**（防存储型 XSS）
- 统一 Mess 返回；工具错误必须按 §6 转译，原始 stderr 只进日志
- 幂等：source_url 查重 + 任务状态机单向前转
- 资源：下载单并发、转写并发=CPU-1；子进程强注册强回收（进程数 Gauge 兜底）
- 删除资源必须删全套（音频+封面+歌词+DB+关联），危险操作前端确认框

## 17. 开发阶段

| 阶段 | 内容 |
|---|---|
| P1 | Compose 基建：mysql/redis/nacos + 现有服务容器化跑通（localhost→服务名改造） |
| P2 | Mod_media：执行器 + ToolsLocator + 下载管线 + 412 |
| P3 | 响度 + 字幕 + AI转写 + 工具/模型管理（含自动更新） |
| P4 | 播放统计 + 设置中心 |
| P5 | Prometheus/Grafana/Alertmanager + 自定义指标 + 看板 + 告警联调 |
| P6 | Sentinel 熔断限流 + 任务自愈 + 故障演练 |

## 18. 必带的前车之鉴（源项目实测）

1. 子进程同步调用冻结整个服务——全异步
2. shell 拼接命令遇特殊字符必炸——数组参数
3. B站 412 = 旧版 yt-dlp + 缺 buvid cookie，两层方案缺一不可
4. ffmpeg ebur128 结果在 stderr
5. 大模型文件必须用户确认才下载，.part + 体积校验防半截
6. 取消任务 = kill + 清 `*_temp*`，否则半成品无限堆积
7. JVM/脚本统一 UTF-8（Windows 下尤其）
8. 删除要删全套，否则孤儿文件占磁盘
9. 长任务必须进度 + 可取消，否则被判"卡死"
10. 前端弹层触发事件要 stopPropagation（浮层闪关）

## 附录：源码索引（去 Sakura 仓库抄哪里）
命令构造/网络注入/B站cookie：main.js getNetworkArgs/getBilibiliCookies ｜ 进程执行器：main.js executeCommand ｜ 下载管线/错误转译：downloadBilibiliVideo ｜ 响度解析：analyzeSongVolume ｜ 字幕→LRC：lyrics.js ｜ 工具下载器：tools-manager.js ｜ 业务SQL全家：database.js
